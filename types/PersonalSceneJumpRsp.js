"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _PersonalSceneJumpRsp_one_of_decls;
Object.defineProperty(exports, "__esModule", { value: true });
exports.PersonalSceneJumpRsp = void 0;
/**
 * Generated by the protoc-gen-ts.  DO NOT EDIT!
 * compiler version: 3.20.3
 * source: PersonalSceneJumpRsp.proto
 * git: https://github.com/thesayyn/protoc-gen-ts */
const dependency_1 = require("./Vector");
const pb_1 = require("google-protobuf");
class PersonalSceneJumpRsp extends pb_1.Message {
    constructor(data) {
        super();
        _PersonalSceneJumpRsp_one_of_decls.set(this, []);
        pb_1.Message.initialize(this, Array.isArray(data) ? data : [], 0, -1, [], __classPrivateFieldGet(this, _PersonalSceneJumpRsp_one_of_decls, "f"));
        if (!Array.isArray(data) && typeof data == "object") {
            if ("dest_scene_id" in data && data.dest_scene_id != undefined) {
                this.dest_scene_id = data.dest_scene_id;
            }
            if ("retcode" in data && data.retcode != undefined) {
                this.retcode = data.retcode;
            }
            if ("dest_pos" in data && data.dest_pos != undefined) {
                this.dest_pos = data.dest_pos;
            }
        }
    }
    get dest_scene_id() {
        return pb_1.Message.getFieldWithDefault(this, 5, 0);
    }
    set dest_scene_id(value) {
        pb_1.Message.setField(this, 5, value);
    }
    get retcode() {
        return pb_1.Message.getFieldWithDefault(this, 8, 0);
    }
    set retcode(value) {
        pb_1.Message.setField(this, 8, value);
    }
    get dest_pos() {
        return pb_1.Message.getWrapperField(this, dependency_1.Vector, 11);
    }
    set dest_pos(value) {
        pb_1.Message.setWrapperField(this, 11, value);
    }
    get has_dest_pos() {
        return pb_1.Message.getField(this, 11) != null;
    }
    static fromObject(data) {
        const message = new PersonalSceneJumpRsp({});
        if (data.dest_scene_id != null) {
            message.dest_scene_id = data.dest_scene_id;
        }
        if (data.retcode != null) {
            message.retcode = data.retcode;
        }
        if (data.dest_pos != null) {
            message.dest_pos = dependency_1.Vector.fromObject(data.dest_pos);
        }
        return message;
    }
    toObject() {
        const data = {};
        if (this.dest_scene_id != null) {
            data.dest_scene_id = this.dest_scene_id;
        }
        if (this.retcode != null) {
            data.retcode = this.retcode;
        }
        if (this.dest_pos != null) {
            data.dest_pos = this.dest_pos.toObject();
        }
        return data;
    }
    serialize(w) {
        const writer = w || new pb_1.BinaryWriter();
        if (this.dest_scene_id != 0)
            writer.writeUint32(5, this.dest_scene_id);
        if (this.retcode != 0)
            writer.writeInt32(8, this.retcode);
        if (this.has_dest_pos)
            writer.writeMessage(11, this.dest_pos, () => this.dest_pos.serialize(writer));
        if (!w)
            return writer.getResultBuffer();
    }
    static deserialize(bytes) {
        const reader = bytes instanceof pb_1.BinaryReader ? bytes : new pb_1.BinaryReader(bytes), message = new PersonalSceneJumpRsp();
        while (reader.nextField()) {
            if (reader.isEndGroup())
                break;
            switch (reader.getFieldNumber()) {
                case 5:
                    message.dest_scene_id = reader.readUint32();
                    break;
                case 8:
                    message.retcode = reader.readInt32();
                    break;
                case 11:
                    reader.readMessage(message.dest_pos, () => message.dest_pos = dependency_1.Vector.deserialize(reader));
                    break;
                default: reader.skipField();
            }
        }
        return message;
    }
    serializeBinary() {
        return this.serialize();
    }
    static deserializeBinary(bytes) {
        return PersonalSceneJumpRsp.deserialize(bytes);
    }
}
exports.PersonalSceneJumpRsp = PersonalSceneJumpRsp;
_PersonalSceneJumpRsp_one_of_decls = new WeakMap();
