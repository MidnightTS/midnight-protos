"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _BounceConjuringSettleNotify_one_of_decls;
Object.defineProperty(exports, "__esModule", { value: true });
exports.BounceConjuringSettleNotify = void 0;
/**
 * Generated by the protoc-gen-ts.  DO NOT EDIT!
 * compiler version: 3.20.3
 * source: BounceConjuringSettleNotify.proto
 * git: https://github.com/thesayyn/protoc-gen-ts */
const dependency_1 = require("./BounceConjuringGallerySettleInfo");
const pb_1 = require("google-protobuf");
class BounceConjuringSettleNotify extends pb_1.Message {
    constructor(data) {
        super();
        _BounceConjuringSettleNotify_one_of_decls.set(this, []);
        pb_1.Message.initialize(this, Array.isArray(data) ? data : [], 0, -1, [], __classPrivateFieldGet(this, _BounceConjuringSettleNotify_one_of_decls, "f"));
        if (!Array.isArray(data) && typeof data == "object") {
            if ("is_new_record" in data && data.is_new_record != undefined) {
                this.is_new_record = data.is_new_record;
            }
            if ("settle_info_map" in data && data.settle_info_map != undefined) {
                this.settle_info_map = data.settle_info_map;
            }
            if ("total_score" in data && data.total_score != undefined) {
                this.total_score = data.total_score;
            }
            if ("chapter_id" in data && data.chapter_id != undefined) {
                this.chapter_id = data.chapter_id;
            }
        }
        if (!this.settle_info_map)
            this.settle_info_map = new Map();
    }
    get is_new_record() {
        return pb_1.Message.getFieldWithDefault(this, 14, false);
    }
    set is_new_record(value) {
        pb_1.Message.setField(this, 14, value);
    }
    get settle_info_map() {
        return pb_1.Message.getField(this, 4);
    }
    set settle_info_map(value) {
        pb_1.Message.setField(this, 4, value);
    }
    get total_score() {
        return pb_1.Message.getFieldWithDefault(this, 2, 0);
    }
    set total_score(value) {
        pb_1.Message.setField(this, 2, value);
    }
    get chapter_id() {
        return pb_1.Message.getFieldWithDefault(this, 13, 0);
    }
    set chapter_id(value) {
        pb_1.Message.setField(this, 13, value);
    }
    static fromObject(data) {
        const message = new BounceConjuringSettleNotify({});
        if (data.is_new_record != null) {
            message.is_new_record = data.is_new_record;
        }
        if (typeof data.settle_info_map == "object") {
            message.settle_info_map = new Map(Object.entries(data.settle_info_map).map(([key, value]) => [Number(key), dependency_1.BounceConjuringGallerySettleInfo.fromObject(value)]));
        }
        if (data.total_score != null) {
            message.total_score = data.total_score;
        }
        if (data.chapter_id != null) {
            message.chapter_id = data.chapter_id;
        }
        return message;
    }
    toObject() {
        const data = {};
        if (this.is_new_record != null) {
            data.is_new_record = this.is_new_record;
        }
        if (this.settle_info_map.size > 0) {
            data.settle_info_map = (Object.fromEntries)((Array.from)(this.settle_info_map).map(([key, value]) => [key, value.toObject()]));
        }
        if (this.total_score != null) {
            data.total_score = this.total_score;
        }
        if (this.chapter_id != null) {
            data.chapter_id = this.chapter_id;
        }
        return data;
    }
    serialize(w) {
        const writer = w || new pb_1.BinaryWriter();
        if (this.is_new_record != false)
            writer.writeBool(14, this.is_new_record);
        for (const [key, value] of this.settle_info_map) {
            writer.writeMessage(4, this.settle_info_map, () => {
                writer.writeUint32(1, key);
                writer.writeMessage(2, value, () => value.serialize(writer));
            });
        }
        if (this.total_score != 0)
            writer.writeUint32(2, this.total_score);
        if (this.chapter_id != 0)
            writer.writeUint32(13, this.chapter_id);
        if (!w)
            return writer.getResultBuffer();
    }
    static deserialize(bytes) {
        const reader = bytes instanceof pb_1.BinaryReader ? bytes : new pb_1.BinaryReader(bytes), message = new BounceConjuringSettleNotify();
        while (reader.nextField()) {
            if (reader.isEndGroup())
                break;
            switch (reader.getFieldNumber()) {
                case 14:
                    message.is_new_record = reader.readBool();
                    break;
                case 4:
                    reader.readMessage(message, () => pb_1.Map.deserializeBinary(message.settle_info_map, reader, reader.readUint32, () => {
                        let value;
                        reader.readMessage(message, () => value = dependency_1.BounceConjuringGallerySettleInfo.deserialize(reader));
                        return value;
                    }));
                    break;
                case 2:
                    message.total_score = reader.readUint32();
                    break;
                case 13:
                    message.chapter_id = reader.readUint32();
                    break;
                default: reader.skipField();
            }
        }
        return message;
    }
    serializeBinary() {
        return this.serialize();
    }
    static deserializeBinary(bytes) {
        return BounceConjuringSettleNotify.deserialize(bytes);
    }
}
exports.BounceConjuringSettleNotify = BounceConjuringSettleNotify;
_BounceConjuringSettleNotify_one_of_decls = new WeakMap();
