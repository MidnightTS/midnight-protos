"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _EvtAiSyncSkillCdNotify_one_of_decls;
Object.defineProperty(exports, "__esModule", { value: true });
exports.EvtAiSyncSkillCdNotify = void 0;
/**
 * Generated by the protoc-gen-ts.  DO NOT EDIT!
 * compiler version: 3.20.3
 * source: EvtAiSyncSkillCdNotify.proto
 * git: https://github.com/thesayyn/protoc-gen-ts */
const dependency_1 = require("./AiSkillCdInfo");
const pb_1 = require("google-protobuf");
class EvtAiSyncSkillCdNotify extends pb_1.Message {
    constructor(data) {
        super();
        _EvtAiSyncSkillCdNotify_one_of_decls.set(this, []);
        pb_1.Message.initialize(this, Array.isArray(data) ? data : [], 0, -1, [], __classPrivateFieldGet(this, _EvtAiSyncSkillCdNotify_one_of_decls, "f"));
        if (!Array.isArray(data) && typeof data == "object") {
            if ("ai_cd_map" in data && data.ai_cd_map != undefined) {
                this.ai_cd_map = data.ai_cd_map;
            }
        }
        if (!this.ai_cd_map)
            this.ai_cd_map = new Map();
    }
    get ai_cd_map() {
        return pb_1.Message.getField(this, 7);
    }
    set ai_cd_map(value) {
        pb_1.Message.setField(this, 7, value);
    }
    static fromObject(data) {
        const message = new EvtAiSyncSkillCdNotify({});
        if (typeof data.ai_cd_map == "object") {
            message.ai_cd_map = new Map(Object.entries(data.ai_cd_map).map(([key, value]) => [Number(key), dependency_1.AiSkillCdInfo.fromObject(value)]));
        }
        return message;
    }
    toObject() {
        const data = {};
        if (this.ai_cd_map.size > 0) {
            data.ai_cd_map = (Object.fromEntries)((Array.from)(this.ai_cd_map).map(([key, value]) => [key, value.toObject()]));
        }
        return data;
    }
    serialize(w) {
        const writer = w || new pb_1.BinaryWriter();
        for (const [key, value] of this.ai_cd_map) {
            writer.writeMessage(7, this.ai_cd_map, () => {
                writer.writeUint32(1, key);
                writer.writeMessage(2, value, () => value.serialize(writer));
            });
        }
        if (!w)
            return writer.getResultBuffer();
    }
    static deserialize(bytes) {
        const reader = bytes instanceof pb_1.BinaryReader ? bytes : new pb_1.BinaryReader(bytes), message = new EvtAiSyncSkillCdNotify();
        while (reader.nextField()) {
            if (reader.isEndGroup())
                break;
            switch (reader.getFieldNumber()) {
                case 7:
                    reader.readMessage(message, () => pb_1.Map.deserializeBinary(message.ai_cd_map, reader, reader.readUint32, () => {
                        let value;
                        reader.readMessage(message, () => value = dependency_1.AiSkillCdInfo.deserialize(reader));
                        return value;
                    }));
                    break;
                default: reader.skipField();
            }
        }
        return message;
    }
    serializeBinary() {
        return this.serialize();
    }
    static deserializeBinary(bytes) {
        return EvtAiSyncSkillCdNotify.deserialize(bytes);
    }
}
exports.EvtAiSyncSkillCdNotify = EvtAiSyncSkillCdNotify;
_EvtAiSyncSkillCdNotify_one_of_decls = new WeakMap();
