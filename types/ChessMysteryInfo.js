"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _ChessMysteryInfo_one_of_decls;
Object.defineProperty(exports, "__esModule", { value: true });
exports.ChessMysteryInfo = void 0;
/**
 * Generated by the protoc-gen-ts.  DO NOT EDIT!
 * compiler version: 3.20.3
 * source: ChessMysteryInfo.proto
 * git: https://github.com/thesayyn/protoc-gen-ts */
const dependency_1 = require("./ChessEntranceDetailInfo");
const pb_1 = require("google-protobuf");
class ChessMysteryInfo extends pb_1.Message {
    constructor(data) {
        super();
        _ChessMysteryInfo_one_of_decls.set(this, []);
        pb_1.Message.initialize(this, Array.isArray(data) ? data : [], 0, -1, [3], __classPrivateFieldGet(this, _ChessMysteryInfo_one_of_decls, "f"));
        if (!Array.isArray(data) && typeof data == "object") {
            if ("entrance_point_map" in data && data.entrance_point_map != undefined) {
                this.entrance_point_map = data.entrance_point_map;
            }
            if ("exit_point_id_list" in data && data.exit_point_id_list != undefined) {
                this.exit_point_id_list = data.exit_point_id_list;
            }
            if ("detail_info_map" in data && data.detail_info_map != undefined) {
                this.detail_info_map = data.detail_info_map;
            }
        }
        if (!this.entrance_point_map)
            this.entrance_point_map = new Map();
        if (!this.detail_info_map)
            this.detail_info_map = new Map();
    }
    get entrance_point_map() {
        return pb_1.Message.getField(this, 13);
    }
    set entrance_point_map(value) {
        pb_1.Message.setField(this, 13, value);
    }
    get exit_point_id_list() {
        return pb_1.Message.getFieldWithDefault(this, 3, []);
    }
    set exit_point_id_list(value) {
        pb_1.Message.setField(this, 3, value);
    }
    get detail_info_map() {
        return pb_1.Message.getField(this, 5);
    }
    set detail_info_map(value) {
        pb_1.Message.setField(this, 5, value);
    }
    static fromObject(data) {
        const message = new ChessMysteryInfo({});
        if (typeof data.entrance_point_map == "object") {
            message.entrance_point_map = new Map(Object.entries(data.entrance_point_map).map(([key, value]) => [Number(key), value]));
        }
        if (data.exit_point_id_list != null) {
            message.exit_point_id_list = data.exit_point_id_list;
        }
        if (typeof data.detail_info_map == "object") {
            message.detail_info_map = new Map(Object.entries(data.detail_info_map).map(([key, value]) => [Number(key), dependency_1.ChessEntranceDetailInfo.fromObject(value)]));
        }
        return message;
    }
    toObject() {
        const data = {};
        if (this.entrance_point_map.size > 0) {
            data.entrance_point_map = (Object.fromEntries)(this.entrance_point_map);
        }
        if (this.exit_point_id_list != null) {
            data.exit_point_id_list = this.exit_point_id_list;
        }
        if (this.detail_info_map.size > 0) {
            data.detail_info_map = (Object.fromEntries)((Array.from)(this.detail_info_map).map(([key, value]) => [key, value.toObject()]));
        }
        return data;
    }
    serialize(w) {
        const writer = w || new pb_1.BinaryWriter();
        for (const [key, value] of this.entrance_point_map) {
            writer.writeMessage(13, this.entrance_point_map, () => {
                writer.writeUint32(1, key);
                writer.writeUint32(2, value);
            });
        }
        if (this.exit_point_id_list.length)
            writer.writePackedUint32(3, this.exit_point_id_list);
        for (const [key, value] of this.detail_info_map) {
            writer.writeMessage(5, this.detail_info_map, () => {
                writer.writeUint32(1, key);
                writer.writeMessage(2, value, () => value.serialize(writer));
            });
        }
        if (!w)
            return writer.getResultBuffer();
    }
    static deserialize(bytes) {
        const reader = bytes instanceof pb_1.BinaryReader ? bytes : new pb_1.BinaryReader(bytes), message = new ChessMysteryInfo();
        while (reader.nextField()) {
            if (reader.isEndGroup())
                break;
            switch (reader.getFieldNumber()) {
                case 13:
                    reader.readMessage(message, () => pb_1.Map.deserializeBinary(message.entrance_point_map, reader, reader.readUint32, reader.readUint32));
                    break;
                case 3:
                    message.exit_point_id_list = reader.readPackedUint32();
                    break;
                case 5:
                    reader.readMessage(message, () => pb_1.Map.deserializeBinary(message.detail_info_map, reader, reader.readUint32, () => {
                        let value;
                        reader.readMessage(message, () => value = dependency_1.ChessEntranceDetailInfo.deserialize(reader));
                        return value;
                    }));
                    break;
                default: reader.skipField();
            }
        }
        return message;
    }
    serializeBinary() {
        return this.serialize();
    }
    static deserializeBinary(bytes) {
        return ChessMysteryInfo.deserialize(bytes);
    }
}
exports.ChessMysteryInfo = ChessMysteryInfo;
_ChessMysteryInfo_one_of_decls = new WeakMap();
