"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _FishingScore_one_of_decls;
Object.defineProperty(exports, "__esModule", { value: true });
exports.FishingScore = void 0;
/**
 * Generated by the protoc-gen-ts.  DO NOT EDIT!
 * compiler version: 3.20.3
 * source: FishingScore.proto
 * git: https://github.com/thesayyn/protoc-gen-ts */
const pb_1 = require("google-protobuf");
class FishingScore extends pb_1.Message {
    constructor(data) {
        super();
        _FishingScore_one_of_decls.set(this, []);
        pb_1.Message.initialize(this, Array.isArray(data) ? data : [], 0, -1, [], __classPrivateFieldGet(this, _FishingScore_one_of_decls, "f"));
        if (!Array.isArray(data) && typeof data == "object") {
            if ("fishing_score_" in data && data.fishing_score_ != undefined) {
                this.fishing_score_ = data.fishing_score_;
            }
            if ("is_new_record" in data && data.is_new_record != undefined) {
                this.is_new_record = data.is_new_record;
            }
        }
    }
    get fishing_score_() {
        return pb_1.Message.getFieldWithDefault(this, 2, 0);
    }
    set fishing_score_(value) {
        pb_1.Message.setField(this, 2, value);
    }
    get is_new_record() {
        return pb_1.Message.getFieldWithDefault(this, 4, false);
    }
    set is_new_record(value) {
        pb_1.Message.setField(this, 4, value);
    }
    static fromObject(data) {
        const message = new FishingScore({});
        if (data.fishing_score_ != null) {
            message.fishing_score_ = data.fishing_score_;
        }
        if (data.is_new_record != null) {
            message.is_new_record = data.is_new_record;
        }
        return message;
    }
    toObject() {
        const data = {};
        if (this.fishing_score_ != null) {
            data.fishing_score_ = this.fishing_score_;
        }
        if (this.is_new_record != null) {
            data.is_new_record = this.is_new_record;
        }
        return data;
    }
    serialize(w) {
        const writer = w || new pb_1.BinaryWriter();
        if (this.fishing_score_ != 0)
            writer.writeUint32(2, this.fishing_score_);
        if (this.is_new_record != false)
            writer.writeBool(4, this.is_new_record);
        if (!w)
            return writer.getResultBuffer();
    }
    static deserialize(bytes) {
        const reader = bytes instanceof pb_1.BinaryReader ? bytes : new pb_1.BinaryReader(bytes), message = new FishingScore();
        while (reader.nextField()) {
            if (reader.isEndGroup())
                break;
            switch (reader.getFieldNumber()) {
                case 2:
                    message.fishing_score_ = reader.readUint32();
                    break;
                case 4:
                    message.is_new_record = reader.readBool();
                    break;
                default: reader.skipField();
            }
        }
        return message;
    }
    serializeBinary() {
        return this.serialize();
    }
    static deserializeBinary(bytes) {
        return FishingScore.deserialize(bytes);
    }
}
exports.FishingScore = FishingScore;
_FishingScore_one_of_decls = new WeakMap();
